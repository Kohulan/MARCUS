# MARCUS Security Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# REQUIRED SECURITY SETTINGS
# =============================================================================

# OpenAI API Key (Required for AI features)
# Get your API key from: https://platform.openai.com/api-keys
# Format: sk-[48 characters]
OPENAI_API_KEY=sk-your-openai-api-key-here

# Secret Key for Session Encryption (Required)
# Generate a strong random key (32+ characters recommended)
# You can generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-strong-secret-key-for-session-encryption

# =============================================================================
# CORS SECURITY CONFIGURATION
# =============================================================================

# Allowed CORS Origins (Required for production)
# Comma-separated list of allowed origins
# NEVER use "*" in production!
# Examples:
#   Development: http://localhost:8080,http://localhost:3000
#   Production: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=http://localhost:8080

# =============================================================================
# RATE LIMITING CONFIGURATION
# =============================================================================

# Enable/disable rate limiting
# Set to "false" only for development/testing
RATE_LIMIT_ENABLED=true

# Custom rate limiting rules (Optional, JSON format)
# Override default rules for specific endpoint types
# Format: {"endpoint_type": {"requests": N, "window": seconds, "penalty": seconds}}
# Example:
# RATE_LIMIT_RULES={"upload": {"requests": 3, "window": 60, "penalty": 120}, "process": {"requests": 5, "window": 60, "penalty": 60}}

# =============================================================================
# SESSION SECURITY CONFIGURATION
# =============================================================================

# Session timeout in hours
# Default: 24 hours
SESSION_TIMEOUT_HOURS=24

# =============================================================================
# AI MODEL CONFIGURATION
# =============================================================================

# OpenAI Model ID to use
# Can be a standard model (gpt-4, gpt-3.5-turbo) or your fine-tuned model ID
OPENAI_MODEL_ID=gpt-4

# =============================================================================
# LOGGING AND MONITORING
# =============================================================================

# Security event logging level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
SECURITY_LOG_LEVEL=INFO

# =============================================================================
# DEVELOPMENT SETTINGS (DO NOT USE IN PRODUCTION)
# =============================================================================

# Development mode flag
# Set to "production" for production deployments
NODE_ENV=development

# =============================================================================
# PRODUCTION SECURITY CHECKLIST
# =============================================================================
# Before deploying to production, ensure:
# 
# ✅ OPENAI_API_KEY is set to your actual OpenAI API key
# ✅ SECRET_KEY is a strong, random 32+ character string
# ✅ CORS_ORIGINS contains only your actual domain(s), not "*"
# ✅ RATE_LIMIT_ENABLED is set to "true"
# ✅ NODE_ENV is set to "production"
# ✅ This .env file is not committed to version control
# ✅ HTTPS is enforced on your domain
# ✅ All default passwords/keys have been changed
# 
# Security Notes:
# - Never commit this file to version control
# - Use environment-specific .env files (.env.production, .env.staging)
# - Regularly rotate your API keys and secret keys
# - Monitor security logs for suspicious activity
# - Keep dependencies updated for security patches
# 
# =============================================================================

# Example production configuration:
# OPENAI_API_KEY=sk-proj-abc123...your-actual-key...xyz789
# SECRET_KEY=super-secure-random-string-32-chars-minimum-abcdef123456
# CORS_ORIGINS=https://yourdomain.com,https://api.yourdomain.com
# RATE_LIMIT_ENABLED=true
# SESSION_TIMEOUT_HOURS=24
# NODE_ENV=production
# SECURITY_LOG_LEVEL=WARNING
